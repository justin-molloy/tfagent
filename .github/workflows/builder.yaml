
name: initial-test-workflow

on:
  push:
    branches: [ "newdev" ]
  pull_request:
    branches: [ "newdev" ]


jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.5'

      - name: Build
        run: go build -v ./...

      - name: Test
        run: go test -v ./...
